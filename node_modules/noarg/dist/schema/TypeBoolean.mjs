import { TypeCore } from "./TypeCore.mjs";
import { ResultErr, ResultOk } from "./result.mjs";
export class TypeBoolean extends TypeCore {
    constructor() {
        super(...arguments);
        this.name = 'boolean';
    }
    checkType(value) {
        value = value.trim().toLowerCase();
        if (value === 'true' || value === 'yes')
            return new ResultOk(true);
        if (value === 'false' || value === 'no')
            return new ResultOk(false);
        return new ResultErr(`\`${value}\` is not a valid boolean`);
    }
}

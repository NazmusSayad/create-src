"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.default = default_1;
const colors_1 = __importDefault(require("../lib/colors.cjs"));
function default_1(name, booleanFalseEnding) {
    if (name.length == 0) {
        throw new Error(`Flag name cannot be empty`);
    }
    if (name.includes(' ')) {
        throw new Error(`Flag \`${colors_1.default.cyan(name)}\` can not contain spaces`);
    }
    if (name.startsWith('-')) {
        throw new Error(`Flag \`${colors_1.default.cyan(name)}\` should not start with '-'`);
    }
    if (name.includes('=')) {
        throw new Error(`Flag \`${colors_1.default.cyan(name)}\` should not contain '='`);
    }
    if (booleanFalseEnding && name.endsWith(booleanFalseEnding)) {
        throw new Error(`Flag \`${colors_1.default.cyan(name)}\` should not end with \`${booleanFalseEnding}\``);
    }
}

import { CellValue } from "cli-table3";
type CustomTableItems<TArray extends CustomTableSize[]> = CellValue[] & {
    length: TArray['length'];
};
type CustomTableSize = {
    flex: number;
    minWidth?: number;
    maxWidth?: number;
};
export declare function CustomTable<const TWidths extends CustomTableSize[]>({ sizes, ...config }: {
    sizes: TWidths;
    border?: boolean;
}, ...items: CustomTableItems<TWidths>[]): void;
export {};

export function getArrayLengthStr(minLength, maxLength) {
    if (minLength && maxLength)
        return minLength + '-' + maxLength;
    if (minLength)
        return minLength + '-' + '∞';
    if (maxLength)
        return 0 + '-' + maxLength;
    return '';
}
